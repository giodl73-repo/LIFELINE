# LIFELINE agent instructions

Read `CONTEXT.md`, then `.roles/ROLE.md`, before changing the repository.
Build visible household-stability features before reusable infrastructure.
Never infer eligibility, reduce participation into savings, or use person data.
Run `cargo test --all-targets` and the README example before committing.
